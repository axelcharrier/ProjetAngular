import { Routes } from '@angular/router';
import { HomePage, UpdatePage, RegisterPage, LoginPage, ProfilePage } from './helpers/pages-helper';

export const routes: Routes = [HomePage, UpdatePage, RegisterPage, LoginPage, ProfilePage];
